import { styled } from 'styled-components'
import { Entry, Querier } from '../../styles/global'

export const InputPhone = styled(Entry)`
  font-size: 14px;
  text-transform: none;
`

export const InputPhoneBorder = styled(Querier)`
  font-size: 14px;
`